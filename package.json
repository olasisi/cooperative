{
  "name": "cooperative",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @cooperative/contracts && npm run build -w @cooperative/backend && npm run build -w @cooperative/frontend",
    "dev": "npm run dev -w @cooperative/backend",
    "test": "npm run test -w @cooperative/backend && npm run test -w @cooperative/frontend"
  }
}
